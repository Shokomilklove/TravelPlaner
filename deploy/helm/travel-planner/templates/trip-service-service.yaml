apiVersion: v1
kind: Service
metadata:
  name: trip-service
  labels:
    app: trip-service
    {{- include "tp.labels" . | nindent 4 }}
spec:
  selector:
    app: trip-service
  ports:
    - name: http
      port: {{ .Values.tripService.port }}
      targetPort: http
